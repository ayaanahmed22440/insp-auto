// INSP AUTO — reference-led automotive editorial minimalism; Inspection Amber, Carbon/Midnight Navy, evidence-first copy, quiet motion.
import { lazy, useEffect, useState, type FormEvent, type ReactNode } from "react";
import { Menu, X } from "lucide-react";
import { Toaster } from "@/components/ui/sonner";
const Home = lazy(() => import("./pages/Home"));
import NotFound from "./pages/NotFound";
const Pricing = lazy(() => import("./pages/Pricing"));
const CheckoutDetails = lazy(() => import("./pages/CheckoutDetails"));
const VehicleServicePage = lazy(() => import("./pages/VehicleServicePage"));
const AdminApp = lazy(() => import("./pages/Admin"));
const AdminLogin = lazy(() => import("./pages/Admin").then(module => ({ default: module.AdminLogin })));

const services = [["Car History Report", "/services/car-history-report"],["Motorbike History Report", "/services/motorbike-history-report"],["ATV History Report", "/services/atv-history-report"],["Truck History Report", "/services/truck-history-report"],["Boat History Report", "/services/boat-history-report"],["RV History Report", "/services/rv-history-report"]];
function navigate(path: string) { window.history.pushState({}, "", path); window.dispatchEvent(new PopStateEvent("popstate")); window.scrollTo({ top: 0, behavior: "smooth" }); }
function Logo() { return <button className="brand" onClick={() => navigate("/")} aria-label="Home"><img className="brand-logo" src="/assets/insp-auto-logo.jpg" alt="" /></button>; }
function Header() {
  const [open, setOpen] = useState(false); const [servicesOpen, setServicesOpen] = useState(false);
  useEffect(() => { const close = () => setOpen(false); window.addEventListener("popstate", close); return () => window.removeEventListener("popstate", close); }, []);
  return <header className="site-header"><div className="header-inner"><Logo /><nav className={`desktop-nav ${open ? "is-open" : ""}`} aria-label="Primary navigation">
    <button onClick={() => navigate("/")}>Home</button><button onClick={() => navigate("/about")}>About Us</button>
    <div className="nav-dropdown"><button onClick={() => setServicesOpen(!servicesOpen)} aria-expanded={servicesOpen}>Services <span className="chevron">⌄</span></button>{servicesOpen && <div className="dropdown-panel">{services.map(([label, href]) => <button key={href} onClick={() => { navigate(href); setServicesOpen(false); }}>{label}</button>)}</div>}</div>
    <button onClick={() => navigate("/contact")}>Contact Us</button><button onClick={() => navigate("/pricing")}>Pricing</button>
    <div className="nav-dropdown"><button onClick={() => setServicesOpen(!servicesOpen)} aria-expanded={servicesOpen}>Company Policies <span className="chevron">⌄</span></button>{servicesOpen && <div className="dropdown-panel policy-dropdown"><button onClick={() => navigate("/terms")}>Terms & Conditions</button><button onClick={() => navigate("/refund-policy")}>Refund Policy</button><button onClick={() => navigate("/privacy-policy")}>Privacy Policy</button></div>}</div>
  </nav><div className="header-actions"><a className="email-link" href="mailto:support@inspauto.com">support@inspauto.com</a><button className="button button-small" onClick={() => navigate("/pricing")}>Get a Report <span>↗</span></button></div><button className="menu-toggle" onClick={() => setOpen(!open)} aria-label={open ? "Close menu" : "Open menu"}>{open ? <X /> : <Menu />}</button></div>
  <div className={`mobile-nav ${open ? "open" : ""}`}><button onClick={() => { navigate("/"); setOpen(false); }}>Home</button><button onClick={() => { navigate("/about"); setOpen(false); }}>About Us</button><button className="mobile-service-toggle" onClick={() => setServicesOpen(!servicesOpen)}>Services <span>{servicesOpen ? "−" : "+"}</span></button>{servicesOpen && <div className="mobile-service-list">{services.map(([label, href]) => <button key={href} onClick={() => { navigate(href); setOpen(false); }}>{label}</button>)}</div>}<button onClick={() => { navigate("/contact"); setOpen(false); }}>Contact Us</button><button onClick={() => { navigate("/pricing"); setOpen(false); }}>Pricing</button><button onClick={() => { navigate("/terms"); setOpen(false); }}>Terms &amp; Conditions</button><button onClick={() => { navigate("/refund-policy"); setOpen(false); }}>Refund Policy</button><button onClick={() => { navigate("/privacy-policy"); setOpen(false); }}>Privacy Policy</button><a className="button" href="mailto:support@inspauto.com">Email support</a></div></header>;
}
function Footer() { return <footer className="footer"><div className="footer-main"><div className="footer-brand"><Logo /><p>Vehicle history reports designed to help UK buyers make informed decisions.</p><a href="mailto:support@inspauto.com">support@inspauto.com</a></div><div><h3>Company</h3><button onClick={() => navigate("/about")}>About Us</button><button onClick={() => navigate("/services")}>Services</button><button onClick={() => navigate("/contact")}>Contact Us</button></div><div><h3>Services</h3>{services.slice(0,3).map(([label, href]) => <button key={href} onClick={() => navigate(href)}>{label}</button>)}</div><div><h3>Policies</h3><button onClick={() => navigate("/terms")}>Terms & Conditions</button><button onClick={() => navigate("/refund-policy")}>Refund Policy</button><button onClick={() => navigate("/privacy-policy")}>Privacy Policy</button></div></div><div className="footer-bottom"><span>© {new Date().getFullYear()} All rights reserved.</span><span>Built for clearer vehicle decisions.</span></div></footer>; }
function PolicyPage({ kind }: { kind: "terms" | "refund-policy" | "privacy-policy" }) {
 const content = { terms:{title:"Terms & Conditions",intro:"These general website terms describe how INSP AUTO presents and fulfils vehicle history reports. They should be reviewed by a qualified UK legal professional before being relied upon as final legal terms.",sections:[["Service description","INSP AUTO provides vehicle history report products for buyers and other customers who want supporting information before a vehicle purchase."],["Report information","A selected report may include vehicle specifications, title information, recalls, accident information, ownership indicators, finance or lien information, and other available records. Coverage depends on the selected product and the data available to the report provider."],["Accuracy and third-party data","Reports may use information supplied by third-party sources. We do not promise that every record is complete, current, or error-free, and a report is not a substitute for an independent inspection or professional advice."],["Customer responsibilities","Customers are responsible for entering a correct VIN or registration and for checking that the returned vehicle details correspond to the vehicle they are considering."],["Payment and delivery","Payment is completed through the checkout provider shown on the website. Report processing and delivery follow the fulfilment process applicable to the selected product; no instant delivery promise is made unless stated for that product."],["Liability and governing law","To the extent permitted by law, INSP AUTO is not responsible for decisions made solely from a report. These terms are intended for a UK service and are subject to applicable law in England and Wales."]]},"refund-policy":{title:"Refund Policy",intro:"This policy explains the circumstances in which a customer may contact INSP AUTO about a payment or report order. It is general website information and should be reviewed professionally before final publication.",sections:[["Payment and processing","Payments are handled by the selected checkout provider. An order enters processing after payment confirmation is received by the fulfilment process."],["Incorrect vehicle details","Please check the VIN or registration before checkout. A report may not be refundable where the customer supplied incorrect or incomplete vehicle details, subject to applicable consumer rights."],["Failed fulfilment","If INSP AUTO cannot fulfil a paid report, contact support@inspauto.com with the order details so the issue can be reviewed and an appropriate remedy considered."],["Duplicate purchases","If the same product was purchased more than once by mistake, contact support promptly. Duplicate order requests are assessed using the available payment and fulfilment records."],["Contacting support","Refund questions should include the order number, email used at checkout, and a brief description of the issue. Any approved refund is returned through the original payment route where possible."]]},"privacy-policy":{title:"Privacy Policy",intro:"This GDPR-conscious outline explains the information INSP AUTO may process when you use the website. A qualified UK privacy professional should review it before it is adopted as the final policy.",sections:[["Information collected","We may receive your name, email address, VIN or registration, order details, support correspondence, and information needed to investigate a report request."],["Payments and third parties","Payment information is handled by the checkout provider. INSP AUTO does not ask customers to send full card details by email. Third-party providers may process information under their own privacy notices."],["Cookies and analytics","The website may use essential cookies or similar technologies for basic operation. Any analytics should be configured transparently and limited to the purposes described at the point of collection."],["Retention and security","Information is retained only for as long as reasonably needed for fulfilment, support, legal, accounting, or fraud-prevention purposes, with appropriate organisational and technical safeguards."],["Your rights","Subject to applicable law, you may ask about access, correction, deletion, restriction, portability, or objection. Contact support@inspauto.com for an initial request."]]}}[kind];
 return <main className="policy-page"><div className="container narrow"><span className="eyebrow">INSP AUTO / POLICY</span><h1>{content.title}</h1><p className="policy-intro">{content.intro}</p><div className="policy-sections">{content.sections.map(([heading,text]) => <section key={heading}><h2>{heading}</h2><p>{text}</p></section>)}</div><button className="button" onClick={() => navigate("/contact")}>Questions about this page <span>↗</span></button></div></main>;
}
function ServicePage({ label }: { label: string }) { return <main className="detail-page"><div className="container narrow"><span className="eyebrow">INSP AUTO / SERVICE</span><h1>{label}</h1><p className="lead">Useful vehicle information for the decision in front of you.</p><p>Choose the report that matches the vehicle you are considering. Information available in your selected report may include vehicle specifications, title information, safety recalls, accident information, ownership indicators, finance or lien information, and other available records.</p><div className="detail-callout"><span className="stamp">INSP</span><div><strong>Keep the evidence close.</strong><p>Reports are designed to be clear, practical, and easy to discuss with a seller or support team.</p></div></div><button className="button" onClick={() => navigate("/pricing")}>View report options <span>↗</span></button></div></main>; }
function OrderStatusPage() { const [email,setEmail]=useState(""); const [paymentReference,setPaymentReference]=useState(""); const [status,setStatus]=useState<string|null>(null); const [loading,setLoading]=useState(false); const submit=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setStatus(null);try{const response=await fetch("/api/order-status",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,paymentReference})});const payload=await response.json();if(!response.ok||!payload?.ok)throw new Error(payload?.message||"Unable to find the order.");const order=payload.data;setStatus(`Order ${order.id}: ${order.status}`);}catch(error){setStatus(error instanceof Error?error.message:"Unable to find the order.");}finally{setLoading(false);}};return <main className="detail-page"><div className="container narrow"><span className="eyebrow">INSP AUTO / ORDER STATUS</span><h1>Check your order.</h1><p className="lead">Enter the email used at checkout and your payment reference.</p><form onSubmit={submit} className="order-status-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><label>Payment reference<input value={paymentReference} onChange={e=>setPaymentReference(e.target.value)} required /></label><button className="button" type="submit" disabled={loading}>{loading?"Checking…":"Check status"}</button>{status&&<p role="status">{status}</p>}</form></div></main>; }
export default function App() {
 const [path,setPath]=useState(window.location.pathname); useEffect(()=>{const onPopState=()=>setPath(window.location.pathname);window.addEventListener("popstate",onPopState);return()=>window.removeEventListener("popstate",onPopState);},[]); const service=services.find(([,href])=>href===path); let page:ReactNode;
 if(path==="/")page=<Home/>; else if(path==="/pricing")page=<Pricing/>; else if(path==="/checkout-details")page=<CheckoutDetails/>; else if(path==="/about")page=<ServicePage label="About INSP AUTO"/>; else if(path==="/services")page=<Home initialSection="services"/>; else if(path==="/contact")page=<ServicePage label="Contact INSP AUTO"/>; else if(path==="/terms")page=<PolicyPage kind="terms"/>; else if(path==="/refund-policy")page=<PolicyPage kind="refund-policy"/>; else if(path==="/privacy-policy")page=<PolicyPage kind="privacy-policy"/>; else if(path==="/order-status")page=<OrderStatusPage/>; else if(service)page=<VehicleServicePage label={service[0]}/>; else page=<NotFound/>;
 return <><Header/>{page}<Footer/><Toaster/></>;
}
